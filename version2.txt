this is the text file which i make for understanding git.

import { ChatClient, ChatPeerState, ChatUserStatus } from "./PhotonChat/ChatClient";

const client = new ChatClient();
client.connect("your-app-id", "your-app-version");

client.addEventListener("OnChatStateChange", (state: ChatPeerState) => {
    console.log(`Chat state changed to: ${ChatPeerState[state]}`);
});

client.addEventListener("OnUserStatusUpdate", (status: ChatUserStatus) => {
    console.log(`User status changed to: ${ChatUserStatus[status]}`);
});

function sendChatMessage(message: string) {
    client.publishMessage("general", message);
}

client.addEventListener("OnChatMessage", (channel: string, sender: string, message: string) => {
    console.log(`${sender} said: ${message}`);

    const labelNode = cc.find("Canvas/Label");
    labelNode.getComponent(cc.Label).string += `${sender}: ${message}\n`;
});

sendChatMessage("Hello, world!");

